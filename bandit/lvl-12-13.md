The password for the next level is stored in the file data.txt, which is a hexdump of a file that has been repeatedly compressed. For this level it may be useful to create a directory under /tmp in which you can work. Use mkdir with a hard to guess directory name. Or better, use the command “mktemp -d”. Then copy the datafile using cp, and rename it using mv (read the manpages!)

1: cpied data.txt into another tmp folder
bandit12@bandit:~$ ls
data.txt
bandit12@bandit:~$ cp data.txt /tmp/tmp.Lm335Fnkef
bandit12@bandit:~$ cd /tmp/tmp.Lm335Fnkef
bandit12@bandit:/tmp/tmp.Lm335Fnkef$ ls
data.txt
bandit12@bandit:/tmp/tmp.Lm335Fnkef$ mv data.txt pokusaj1.txt

2: saw an fiele and its not human readable next step is to find out why i cant see it. I tried using file data.txt  but it turns out file is ascii text. 
Then I found out file is either base64 coded, or it is a script or hex dump
Hex Dmp is binary file represented as a text 

3: convert hex dump into binary file

xxd -r data.txt > binary - write result of covering text to binary into bonary file

when i check binary file with file it says its gzip compressed data

4: in orther to unpack data it needs to have right ending
if its bzip2 it needs to end with .gz

so i need to rename it to binary.gz with mv command

5: now binary file is decompressed so i check it again
now its bzip2 compressed data

I do same proces again rename fiel and decompress it

I did it several times and in one moment file for POSIX tar then I used tar -xf data6 ...

password: FO5dwFsc0cbaIiH0h8J2eUks2vdTDwAn